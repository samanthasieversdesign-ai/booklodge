import React from 'react';
import ReactDOM from 'react-dom/client';
import BookLodgeApp from './BookLodge';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BookLodgeApp />
  </React.StrictMode>
);
